@model System.Data.DataTable
@using System.Data;
@{
    ViewBag.Title = "AddBatchUpload";
}


<h2>Add Batch Upload</h2>

@*@using (Html.BeginForm("AddBatchUpload","",FormMethod.Post, new {enctype="multipart/form-data"})){ 

<input type="file" name="FileUpload" />
<input type="submit" name="Submit" id="Submit" value="Upload" />

}   

<p>@Html.Encode(ViewData["Feedback"])</p>*@

@using (Html.BeginForm("AddBatchUpload", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary()

    <div class="form-group">
        <input type="file" id="dataFile" name="upload" />
    </div>

    <div class="form-group">
        <input type="submit" value="Upload" name=" Submit" class="btn btn-default" />
    </div>
    <p>@Html.Encode(ViewData["Feedback"])</p>
    if (Model != null)
    {
        <table>
            <thead>
                <tr>
                    @foreach (DataColumn col in Model.Columns)
                    {
                        <th>@col.ColumnName</th>
                    }
                </tr>
            </thead>
            <tbody>
                @foreach (DataRow row in Model.Rows)
                {
                    <tr>
                        @foreach (DataColumn col in Model.Columns)
                        {
                            <td>@row[col.ColumnName]</td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    }

}